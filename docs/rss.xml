<?xml version="1.0" encoding="UTF-8"?><rss xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:atom="http://www.w3.org/2005/Atom" version="2.0" xmlns:media="http://search.yahoo.com/mrss/"><channel><title><![CDATA[Caden Kraft]]></title><description><![CDATA[Some of my Projects]]></description><link>https://blog.cadenkraft.com/</link><image><url>https://blog.cadenkraft.com/favicon.png</url><title>Caden Kraft</title><link>https://blog.cadenkraft.com/</link></image><generator>Ghost 4.36</generator><lastBuildDate>Wed, 23 Feb 2022 20:57:16 GMT</lastBuildDate><atom:link href="https://blog.cadenkraft.com/rss.xml" rel="self" type="application/rss+xml"/><ttl>60</ttl><item><title><![CDATA[Creating a Ghost Blog with Free Webhosting using Github Pages]]></title><description><![CDATA[Caden Kraft]]></description><link>https://blog.cadenkraft.com/creating-a-ghost-blog-with-free-webhosting/</link><guid isPermaLink="false">62159f49c47878000150f7f0</guid><category><![CDATA[Tutorial]]></category><dc:creator><![CDATA[Caden Kraft]]></dc:creator><pubDate>Wed, 23 Feb 2022 04:58:02 GMT</pubDate><media:content url="https://blog.cadenkraft.com/content/images/2022/02/ghost-blog.png" medium="image"/><content:encoded><![CDATA[<img src="https://blog.cadenkraft.com/content/images/2022/02/ghost-blog.png" alt="Creating a Ghost Blog with Free Webhosting using Github Pages"><p>When I began my search to find the best blog/CMS that I could host at home (or in my dorm room) on my server, I landed on Ghost pretty quickly with its slick UI and broad feature-set including an admin panel, app integrations, and native <a href="https://docs.docker.com/get-started/overview/">Docker</a> support. </p><p>My problems arose when it came to hosting. Although I can host it on my server I currently cannot port-forward traffic to my blog, and it also introduces a pretty significant security risk. &#xA0;</p><p>Bring in Github pages. Github pages is a great way to host websites completely for free with the one cavitate being that they have to be completely static. As Ghost doesn&apos;t natively support this to my knowledge, this is where the guide begins.</p><p><strong>Requirements</strong></p><ul><li>Server/Laptop/PC you have to host the server on for local editing. The final site will be on GitHub pages so this machine doesn&apos;t have to be running 24/7.</li><li>Free Github account</li><li>Basic Linux command line knowledge</li><li>Patience</li></ul><p><strong>Setting up Linux environment</strong></p><p>This guide should work natively on Linux and macOS (untested) and if you only have a Windows machine, it should work using <a href="https://docs.microsoft.com/en-us/windows/wsl/install">WSL2</a>. </p><p>First, we need to install Docker and some other packages. Going forward, I will be listing commands that would be used on Debian/Ubuntu-based systems as that is what I personally used, but they should be similar for other platforms. To install Docker, we can run these commands below.</p><figure class="kg-card kg-code-card"><pre><code class="language-bash">sudo apt-get install ca-certificates curl gnupg lsb-release nano git wget curl</code></pre><figcaption>Download apt packages</figcaption></figure><figure class="kg-card kg-code-card"><pre><code class="language-bash">curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg</code></pre><figcaption>Add Docker GPG key</figcaption></figure><figure class="kg-card kg-code-card"><pre><code class="language-bash">echo \
  &quot;deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable&quot; | sudo tee /etc/apt/sources.list.d/docker.list &gt; /dev/null</code></pre><figcaption>Set up stable repository</figcaption></figure><pre><code class="language-bash">sudo apt-get update
sudo apt-get install docker-ce docker-ce-cli docker-compose containerd.io</code></pre><p>Next, we want to install all of the packages needed for the static site generator that we will be running outside of the Docker container on bare metal.</p><figure class="kg-card kg-code-card"><pre><code class="language-bash">curl -sL https://deb.nodesource.com/setup_14.x | sudo -E bash -</code></pre><figcaption>Download Node.js install script from NodeSource (apt package is really old)</figcaption></figure><figure class="kg-card kg-code-card"><pre><code class="language-bash">sudo apt install nodejs</code></pre><figcaption>Install Node.js through apt now that the source is added (includes both nodejs and npm)</figcaption></figure><p><strong>Setting up the Ghost Docker container</strong></p><p>The method I&apos;m going to be using to set up Ghost with Docker is through docker-compose. Firstly we need to create a docker-compose.yml file.</p><pre><code class="language-bash">nano docker-compose.yaml</code></pre><p> In nano paste and edit the docker-compose install parameters below.</p><figure class="kg-card kg-code-card"><pre><code class="language-docker">version: &apos;3.1&apos;

services:

  ghost:
    image: ghost:4.36.3
    restart: always
    ports:
      - 2368:2368
    environment:
      # see https://ghost.org/docs/config/#configuration-options
      database__client: mysql
      database__connection__host: db
      database__connection__user: root
      database__connection__password: EXAMPLE
      database__connection__database: ghost
      # this url value is just an example, and is likely wrong for your environment!
      url: http://serverip:2368
      # contrary to the default mentioned in the linked documentation, this image defaults to NODE_ENV=production (so development mode needs to be explicitly specified if desired)
      #NODE_ENV: development

  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: EXAMPLE</code></pre><figcaption>docker-compose.yml</figcaption></figure><p>Press CTRL-X to exit and Y to save your changes. Before we start the container, we also want to add your current user to the Docker group on your computer so you will be able to run all of these commands without using sudo.</p><figure class="kg-card kg-code-card"><pre><code class="language-bash">sudo groupadd docker</code></pre><figcaption>Create the Docker group</figcaption></figure><figure class="kg-card kg-code-card"><pre><code class="language-bash">sudo usermod -aG docker $USER</code></pre><figcaption>Add the current user to the Docker group</figcaption></figure><p>You will likely have to log out and log in for these changes to be implemented.</p><p>Finally, we can get into actually starting up the Ghost blog. If you are not running this on a server and are on a laptop or desktop, I recommend using <a href="https://code.visualstudio.com/">VSCode</a> with the Docker extension. The workflow is very nice for managing Docker containers and images.</p><figure class="kg-card kg-image-card"><img src="https://blog.cadenkraft.com/content/images/2022/02/docker-3.png" class="kg-image" alt="Creating a Ghost Blog with Free Webhosting using Github Pages" loading="lazy"></figure><p>Now we need to load up the Docker container we created earlier. Navigate to the directory that you created the docker-compose.yml file in and run</p><pre><code class="language-bash">docker-compose up -d</code></pre><p>This should take a while as it has to download and set up the Ghost Docker image but after it says the website is finished loading you should be able to navigate in your browser to <code>http://localhost:2368</code> or <code>http://serverip:2368</code> depending on your setup. You should see a nice-looking blog template with all the images loaded properly. Then you can add <code>/ghost</code> to the end of your URL to access the admin dashboard. </p><figure class="kg-card kg-image-card"><img src="https://blog.cadenkraft.com/content/images/2022/02/ghost-default.jpg" class="kg-image" alt="Creating a Ghost Blog with Free Webhosting using Github Pages" loading="lazy" width="1400" height="1123" thisisbuggedatm="https://blog.cadenkraft.com/content/images/size/w600/2022/02/ghost-default.jpg 600w, https://blog.cadenkraft.com/content/images/size/w1000/2022/02/ghost-default.jpg 1000w, https://blog.cadenkraft.com/content/images/2022/02/ghost-default.jpg 1400w" sizes="(min-width: 720px) 720px"></figure><p>Here it will prompt you to set up an account and parts of your blog. I&apos;m not going to go too much more in-depth on that side of things as that is more up to the user but if you have everything up to here working we should be good to set up the GitHub pages integration.</p><p><strong>Setting up Github Pages</strong></p><p>Now that we have our local Ghost blog up and running, we want to set the static site will be generated and sent to Github Pages. The tool we will be using is called <a href="https://github.com/Fried-Chicken/ghost-static-site-generator">Ghost-Static-Site-Generator</a>. A big thanks to the creator for making it and all of the useful documentation. To install it, we just run </p><pre><code class="language-bash">npm install -g ghost-static-site-generator</code></pre><p>Next, we need to prepare our git repository on our personal machine. To start, create a folder you want the repository to be in and initialize it. </p><pre><code class="language-bash">mkdir ghost
cd ghost/
git init </code></pre><p>Then we have to go online to Github and create a few things. Starting with the repository, create a repository with any name you like and copy the URL under the &quot;code&quot; dropdown.</p><figure class="kg-card kg-image-card"><img src="https://blog.cadenkraft.com/content/images/2022/02/image-13.png" class="kg-image" alt="Creating a Ghost Blog with Free Webhosting using Github Pages" loading="lazy"></figure><p>We want to add this repository as a remote repository location for git to push the files. We can do this by running </p><pre><code class="language-bash">git remote add origin https://github.com/yourlinkhere.git</code></pre><p>We need to go back to Github and generate a personal access token that the GSSG can use when creating the site to push files to your repo. Go to this <a href="https://github.com/settings/tokens">link here</a> and create a new token with a name of your choice and give it repo permissions. Copy this link and keep it somewhere safe (e.g., not in your git repository folder). Next, we can run the following commands to test everything working.</p><figure class="kg-card kg-code-card"><pre><code class="language-bash">touch test
git add .
git commit -m &quot;first commit&quot;
git push -u origin master
git config --global credential.helper store
</code></pre><figcaption>When prompted for a username and password, use the token created earlier as your passcode.</figcaption></figure><p>You should see a test file in your Github repo and a commit message if all has gone well.</p><p><strong>Set up Ghost-Static-Site-Generator with Github</strong></p><p>Lastly, we need to set up a script to update Github with the latest version of the blog. Luckily, I have made a script for this, which is set up below</p><figure class="kg-card kg-code-card"><pre><code class="language-bash">nano ghost-updater.sh</code></pre><figcaption>Create a script in your git directory</figcaption></figure><figure class="kg-card kg-code-card"><pre><code class="language-bash">#!/bin/bash
date=$(date)
git pull origin master
rm -r docs
mkdir docs
cd docs
echo &quot;www.mydomain.com&quot; &gt; CNAME
cd -
gssg --dest docs
cd docs/
docker cp ghost:/var/lib/ghost/content/images/. content/images
grep -lR &quot;serverip:2368&quot; . | xargs sed -i &apos;s/serverip:2368/www.mydomain.com/g&apos;
cd -
git add .
git commit -m &quot;$date&quot;
git config --global credential.helper store
git push -u origin master
</code></pre><figcaption>Paste the following code into nano and then save and exit.</figcaption></figure><p>Also, I know there are some patchy solutions to some current bugs in Ghost-Static-Site-Generator in this script but I don&apos;t really know how to code well enough to make a proper fork. If you are reading this it is probably still working.</p><figure class="kg-card kg-code-card"><pre><code class="language-bash">sudo chmod +x ghost-updater.sh</code></pre><figcaption>Make the script executable</figcaption></figure><figure class="kg-card kg-code-card"><pre><code class="language-bash">./ghost-updater.sh</code></pre><figcaption>Run the script</figcaption></figure><p>If all of this went successfully, you should have many files in your Github repo now. To set up Github pages, go to the Github pages tab in settings and select the master branch and <code>/docs</code> as your folder. You can set up a custom domain here as well. Just make sure it is the one you have been using in these scripts.</p><p>If your page is bugged out and only showing HTML elements and no CSS or Javascript, you likely put the wrong domain in the script above. If you don&apos;t have a custom domain, the default should be <a href="https://www.youtube.com/watch?v=dQw4w9WgXcQ">https://github-username.github.io/you-repo-name</a>.</p><p>If all of this has worked up till now, congratulations, you&apos;re done! I hope this guide helped get you to that point. &#xA0;</p><p>If I made any mistakes in my guide or updates break the code, <a href="https://github.com/CKraft11/ghost-docker-on-github-pages/issues">please open an issue</a>.</p>]]></content:encoded></item></channel></rss>